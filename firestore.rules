rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Prototype rule: open read/write for the tracker state document.
    // Replace before customer/production use.
    match /financialTrackerProjects/{projectKey}/state/{stateDoc} {
      allow read, write: if true;
    }
  }
}
